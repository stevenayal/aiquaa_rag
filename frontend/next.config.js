/** @type {import('next').NextConfig} */
const nextConfig = {
  // La API Python vive en /api/ en el mismo dominio (Vercel functions)
  // En dev local apuntamos a localhost:3000/api (vercel dev)
}

module.exports = nextConfig
